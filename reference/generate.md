# Generate method

Generic generate function from the generics package. Used internally for
forecast simulation.

## Usage

``` r
generate(x, ...)
```

## See also

[`generate`](https://generics.r-lib.org/reference/generate.html)

## Examples

``` r
# Used internally by forecast methods
```
