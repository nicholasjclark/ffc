comment: false

coverage:
  ignore:
    - "src/stanExports_*.h"
    - "src/stanExports_*.cc"
    - "src/init.cpp"
    - "src/*.o"
    - "src/*.dll"
  status:
    project:
      default:
        target: auto
        threshold: 1%
        informational: true
    patch:
      default:
        target: auto
        threshold: 1%
        informational: true
